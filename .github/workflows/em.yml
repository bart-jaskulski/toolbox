name: em CI

on:
  push:
    paths:
      - "em/**"
      - "go.work"
      - "go.work.sum"
      - ".github/workflows/em.yml"
  pull_request:
    paths:
      - "em/**"
      - "go.work"
      - "go.work.sum"
      - ".github/workflows/em.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: em
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: em/go.mod
      - name: Download dependencies
        run: go mod download
      - name: Test
        run: go test ./...
      - name: Build
        run: go build ./...
