name: bm CI

on:
  push:
    paths:
      - "bm/**"
      - "go.work"
      - "go.work.sum"
      - ".github/workflows/bm.yml"
  pull_request:
    paths:
      - "bm/**"
      - "go.work"
      - "go.work.sum"
      - ".github/workflows/bm.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bm
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: bm/go.mod
      - name: Download dependencies
        run: go mod download
      - name: Test
        run: go test ./...
      - name: Build
        run: go build ./...
